import React from 'react';

/**
 * DEPRECATED: This component relied on eventSuggestionStore which has been removed.
 * Component is not currently used anywhere in the app.
 * Stubbed out to prevent bundling errors.
 * 
 * TODO: Either remove this file entirely or refactor to use the new architecture
 */
export function SuggestedWeaves() {
  return null;
}
